from django.apps import AppConfig


class ProfilesConfig(AppConfig):
    # Default app configuration for the profiles Django app.
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'profiles'
